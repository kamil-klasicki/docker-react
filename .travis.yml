language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t kamilklasicki/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true kamilklasicki/docker-react npm run test
